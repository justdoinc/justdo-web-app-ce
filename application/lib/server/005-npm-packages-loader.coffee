# The following imports some npm packages, just so they'll get picked up even if no
# one is using them, to make them available in $ meteor shell

import { MongoInternals } from "meteor/mongo"
import fs from "fs"